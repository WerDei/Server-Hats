package net.werdei.serverhats.utils;
/*
import net.minecraft.item.Item;
import net.minecraft.tag.ServerTagManagerHolder;
import net.minecraft.tag.Tag;
import net.minecraft.util.Identifier;
import net.minecraft.util.registry.Registry;

public class Tags
{
    public static Identifier getItemTagId(Tag<Item> tag)
    {
        return ServerTagManagerHolder.getTagManager().getTagId(Registry.ITEM_KEY, tag, () ->
                new RuntimeException("Provided item tag is not registered"));
    }

    public static Tag<Item> getItemTag(Identifier id)
    {
        return ServerTagManagerHolder.getTagManager().getTag(Registry.ITEM_KEY, id, (identifier) ->
                new RuntimeException("Unknown item tag '" + identifier + "'"));
    }
}

 */
